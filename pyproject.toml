[build-system]
requires = ["setuptools >= 40.9", "wheel"]
build-backend = "setuptools.build_meta"

[tool.black]
target-version = ["py38"]
line-length = 79

[tool.isort]
profile = "black"
combine_as_imports = true
